#include<stdio.h>

int main()
{
    int A, B, N;
    scanf("%d %d %d", &A, &B, &N);
    if( (A + B) % N == 0){
        printf("%d", (A + B) / N);
    }
    else{
        printf("Invalid");
    }
    return 0;
}