#include<stdio.h>

int main()
{
    int X, Y, N;
    scanf("%d %d %d", &X, &Y, &N);
    if(N % X == 0 && N % Y == 0){
        printf("BOTH");
    }
    else if(N % X == 0){
        printf("%d", X);
    }
    else if(N % Y == 0){
        printf("%d", Y);
    }
    else{
        printf("NONE");
    }
    return 0;
}
