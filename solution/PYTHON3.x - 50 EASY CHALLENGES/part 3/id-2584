S = input()

if '_' in S:
    index_of_underscore = S.index('_')
    reversed_part = S[:index_of_underscore][::-1]
    result = reversed_part + S[index_of_underscore:]
else:
    result = S[::-1]

print(result)
